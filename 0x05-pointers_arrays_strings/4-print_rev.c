#include <stdio.h>
#include <string.h>
#include "main.h"
/**
 *print_rev - Function that reverse a string.
 *Return: Nothing from it.
 */
void print_rev(char *s)
{
int length = strlen(s);
while len(--)
{
putchar(*(s + length));
}
putchar(10);
}
